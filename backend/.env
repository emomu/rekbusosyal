# Sunucu Portu (Localde 5001, Railway bunu otomatik atar)
PORT=5001

# Veritabanı Bağlantısı (Localde bu, Deployda Atlas linki olacak)
MONGO_URI=mongodb://localhost:27017/kbu-sosyal

# Güvenlik Anahtarı (Token oluşturmak için)
JWT_SECRET="cok_gizli_anahtar_kelime"

# Mail Ayarları (Gmail Adresin ve Uygulama Şifren)
EMAIL_USER=infokbusosyal@gmail.com
EMAIL_PASS=bfuf cnup hlzk mcna

# Linkler (Localde localhost, Deployda https://... olacak)
# Backend kendi adresini bilmeli (Mail linki için)
BACKEND_URL=http://localhost:5001
# Backend frontend'i tanımalı (CORS ve Redirect için)
FRONTEND_URL=http://localhost:5173